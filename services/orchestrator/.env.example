# Orchestrator Configuration
# MODE: simulation | approval | autonomous
# - simulation: Plans actions but doesn't execute
# - approval: Creates pending steps, waits for manual approval
# - autonomous: Executes actions automatically (use with caution!)
MODE=approval

# Terminal worker URL
TERMINAL_URL=http://localhost:8002

# OpenAI API Key (optional - for VLM integration)
OPENAI_API_KEY=

# Rate limiting (actions per second)
RATE_LIMIT=2

# Step timeout (seconds)
STEP_TIMEOUT=60
