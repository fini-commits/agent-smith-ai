# Terminal Worker Configuration
# Set ACTIONS_ENABLED=false to run in simulation mode (safe for testing on host)
# Set ACTIONS_ENABLED=true only when running inside a VM/sandbox
ACTIONS_ENABLED=false
